# Бэклог — Warmr Circle Scraper

Открытые задачи и известные проблемы. Сделанное переносим в `WORKLOG.md` и
отсюда удаляем. Новые пункты — сверху внутри раздела.

Формат: **что** — почему важно — где в коде / данные.

---

## План по воронке (порядок работ, решено 2026-09-18)

Воронка: найти → проверить живое и тип входа → название → ICP → прочитать
открытое → вступить → прочитать закрытое → лиды. Порядок ниже выбран по
отдаче: сначала то, что даёт лиды без вступлений и без риска для аккаунта.

1. [ ] **Влить локальную ветку `local/night-2026-09-19` через PR** (в ней
   `fix/enrich-dead-host-names` + ночные правки, см. WORKLOG 2026-09-19). На
   Mac-воркере она уже стоит; любой деплой старого кода вернёт чтение превью
   и задвоит посты. Заодно выключить плановое обогащение на Railway.
2. [~] **Перечитать посты полным текстом.** Открытые (74 сообщества) —
   сделано ночью 2026-09-19. Сообщества с куками дочитывает плановый сбор
   воркера (10 из 28 к 05:45).
3. [x] ~~DNS-остаток пачками~~ — сделано 2026-09-19, курсор 14 354.
4. [~] **Перепроверить тип входа у 5 237 строк** с названием и
   `join_type` unknown/NULL — `join_recheck.py` (scratchpad), идёт с 04:32,
   ≤ 20 запросов/мин, продолжается с места остановки.
5. [ ] **Вступить в бесплатные подходящие** (`free_join`, ~17 в очереди на
   основном аккаунте). Сначала 1–2 для проверки механики, потом включить
   launchd. Перед включением поднять порог правил ICP (маркетинговый мусор).
6. [ ] **Чтение закрытых сообществ по кукам — с Mac**, не с Railway (там
   блок Cloudflare 19 из 26). Первым productizeyourself.
7. [ ] **Приватные (`locked_unknown`, 91 ICP-fit) — НЕ ставить в очередь
   бота целиком.** 4 из 4 проверенных приватные, 1 из них — только после
   входа. Сделать выборку: 5 штук тестовым аккаунтом через бота. Если
   вступить можно в ≤ 1 из 5 — закрыть тему, показывать группой
   «приватные». Если больше — добавить `locked_unknown` в очередь бота с
   низшим приоритетом (`joiner.py`, фильтр `join_type.in_`). Бот уже
   распознаёт «This is a private community» и пишет `invite_skip`.
8. [ ] **Платные (274 ICP-fit)** — решение пользователя, за какие платить.
9. [ ] **Обратная связь «сообщество → лиды»** — считать выход лидов по
   сообществу и источнику, калибровать ICP и приоритеты по нему.

---

## Лиды: качество (ревью ночи 2026-09-19)

Разметка и цифры — в отчёте «Ночной прогон» и в scratchpad сессии
(`held_verdicts.json`, `vini_verdicts.json`, `model_eval.json`,
`prompt_eval.json`).

- [ ] **Почистить Vini** — из 68 отправленных 27 мусор (реклама себя,
  анонсы, статьи, спам 91), 26 — вакансии в штат, 11 — реальные заказы.
  Удалять в Vini руками; у нас — `reject_selected.py` (папка воркера `review/`).
- [ ] **Решение: лид ли вакансия в штат** — правила штрафуют админ-найм
  (`admin_support_hire`), LLM зовёт лидом любую вакансию → результат
  случайный. Зависит от того, кто покупатели в Vini.
- [ ] **Промпт LLM: мусор проходит по определению** — пример «Looking for a
  technical cofounder» в `SYSTEM_PROMPT` делает лидом поиск сооснователей,
  советников, инвесторов. Сравнение на 104 размеченных постах: смена модели
  не помогает (sonnet-5 пропускает 17/40 мусора, gpt-4o-mini 20/40); суженный
  промпт — 3/40 мусора, но теряет 7/48 настоящих. Предложение: модель
  возвращает тип поста (заказ / вакансия / партнёр-инвестор / реклама /
  новость), решение «лид или нет» — в коде по настройкам. `ai_classifier.py`.
- [ ] **Фильтр целевых навыков** — навыки от LLM сравниваются со списком
  точным совпадением, список в промпт не передаётся → отсеян реальный заказ
  видеомонтажёра (лид 36). И наоборот, «Benefits»/«Insurance» находятся в
  любом описании соцпакета → проходят вакансии EA. `meets_requirements`,
  `extract_skills`.
- [ ] **Список исключений бьёт по вакансиям** — одиночные слова «resume»,
  «CV», «AMA» дают −30 («send your resume»). Заменить фразами («my resume»,
  «attached my CV»). `requirements.keywords.exclude`.
- [ ] **`push-leads` не смотрит на `review_status`** — лид, отклонённый в
  дашборде, всё равно уйдёт в Vini. `push_unsynced_leads`.
- [ ] **Вернуть лид по посту 1** (Senior Data/Analytics Engineer, контракт) —
  LLM ночью ошибочно назвал «не лидом», неотправленный лид 2 удалён. При
  повторной проверке та же модель его приняла: ответы нестабильны.
- [ ] **Дубли сообществ** — `practicommunity` и
  `community.practicommunity.com` — одна площадка, один пост дал два лида в
  Vini (40 и 46).
- [ ] **Деньги OpenRouter** — на 2026-09-19 05:45 осталось ~$1 из $10, воркер
  тратит ~$0.4–0.5/ч на полном тексте. Нужен контроль остатка (эндпоинт
  `/api/v1/credits`) и сигнал на дашборде. При отказе LLM лиды по правилам
  теперь держатся на ревью (`fbf48f3`), но качество падает.
- [ ] **`push-leads --force` задвоит лиды в Vini** — внешний ID строится из
  `source_content_id`, который меняется при обновлении превью до полного
  текста.
- [ ] **Десктоп-приложение и `browser_reader` читают превью** — пока не
  исправлены, не запускать ими чтение на проде: добавят строки-превью.
- [ ] **Дубли авторов** — 54 579 авторов на 2 256 имён; новые больше не
  плодятся (75d892c), старые не склеены.

---

## Воронка: качество данных

- [ ] **Десктоп-приложение: те же ошибки?** — проверить в `desktop/src`
  разбор текста поста (превью vs `tiptap_body`) и подпись `locked_unknown`.
- [ ] **Воркер Railway и Mac читают одну очередь** — дублируют работу и
  расходуют лимит Circle. Решить, кто что читает.
- [ ] **91 приватное ICP-fit (`locked_unknown`)** — только вход, бесплатно не
  вступить. Решить: исключить из счётчика «подходят» или показывать
  отдельной группой «приватные».
- [ ] **ICP по одному названию слабый** — у DNS-строк описания почти нет,
  правила и LLM судят по имени. Идея: брать названия разделов и заголовки
  публичных постов как текст для оценки. `classifier/icp_relevance.py`.
- [ ] **Порог правил низкий** — одно слово «startup» (20 баллов) или «ai +
  marketing» дают `icp_flag=true`. Перед включением автоджойна поднять порог,
  иначе бот вступит в «AI Wedding Marketing». `ICP_CONFIDENT_YES`.
- [ ] **Возможные пропуски LLM** на пограничных названиях («The FinTechers»,
  «The Guild of Entrepreneurs», «The Ecosystem Incubator») — проверить после
  добавления текста из постов.
- [ ] **Нет обратной связи «подходит → лиды»** — ICP-fit по замеру даёт лидов
  не больше, чем отвергнутые (выборка 45 лидов, мало). Считать выход лидов по
  сообществу и калибровать ICP по нему.
- [ ] **Тип входа не обновляется при обогащении** — `communities/current`
  отдаёт и название, и тип входа одним запросом, а пишем только название.
  Объединить обогащение и `classify-join-types`.
- [ ] **Десктоп-приложение (`desktop/`)** — проверить, есть ли там те же
  ошибки: фильтр шаблонных названий и `icp_score` для LLM «не подходит».
- [ ] **Сбор затирает цену из каталога Circle** — `harvest.py` (строки
  ~326–332) при каждом чтении пишет `join_type` по живой проверке. При этом
  он не применяет `_price_label_fallback` и не обновляет `join_type_detail`,
  поэтому в detail остаётся «price_label fallback …», а тип уже
  `locked_unknown`/`unknown`.
  - На 2026-09-19 так спрятаны 37 ICP-fit строк с ценой из Discover:
    15 «Free» и 22 платные. На Overview они стоят в «Login screen only»
    (20) и «Could not check yet» (17).
  - Код исправлен в PR #20 (`with_price_label_fallback` для всех, кто
    пишет `join_type`), ждёт мерджа.
  - Осталось: разово пересчитать эти строки после ручной проверки
    пользователя (страница https://claude.ai/artifact/YXSToaZ82gHMaDG85YMWrf).
  - Заодно: 95 из 103 «Login screen only» числятся в «Read», хотя постов у
    них 0. Сбор отмечает приватное сообщество прочитанным (`_mark_synced`).
- [x] **137 постов ни разу не классифицированы** — сделано 2026-09-21:
  `circle-leads classify --use-llm` разобрал все 137, получился 1 лид,
  136 не лиды. Почему плановый сбор их не подбирал, так и не выяснено:
  он классифицирует только те посты, которые читает заново.

## Инфраструктура

- [ ] **Плановое обогащение пошло на второй круг** — 2026-09-19 07:48 курсор
  дошёл до конца и вернулся на 0 (так задумано: «дать недоступным ещё шанс»).
  Для строк без названия это полезно: первый заход (08:48, 183 строки в
  начале таблицы — их раньше обходил Railway, где Cloudflare резал запросы)
  дал 48 новых названий. Но под выборку попадают и 8 972 строки с названием
  без описания — ≈45 часов запросов к Circle ради описаний, которых у
  большинства нет. Решить: второй круг только по строкам без названия.
  `enrich_pending`, `ENRICH_BATCH`.
- [ ] **Воркер падает, если база недоступна при записи итога задачи** —
  02:43 2026-09-19: `complete()` бросил исключение, спас только перезапуск
  launchd; задача осталась RUNNING. Ловить и повторять запись. `cli/main.py`.
- [ ] **Перезапуск воркера посреди сбора теряет сбор до следующего срока** —
  `mark_harvest_run` пишется до `_run_harvest()`, поэтому после рестарта
  сбор не повторится ~6 часов. Писать время после конца или хранить прогресс.
- [ ] **Плановое обогащение на Railway всё ещё включено** — проходит ~5%
  запросов, зря двигает общий курсор `enrichment_cursor_id`. Выключить в
  `cli/main.py` (плановый прогон `enrich_pending(db)`), задеплоить воркер.
- [ ] **Закрытые сообщества с Railway не читаются** — Cloudflare блокирует
  19 из 26 подключений с куками, включая productizeyourself (25 из 39 первых
  лидов). Перенести чтение по кукам на Mac / отдельную машину на домашнем
  интернете.
- [ ] **Дашборд неверно диагностирует блок** — пишет «сессия истекла»,
  хотя куки рабочие и это Cloudflare. Отдельный статус.
- [ ] **Машина 24/7 на домашнем IP** (Mac mini и т.п.) — пока бот, чтение
  по кукам и обогащение живут только при открытом Mac пользователя.
- [ ] **Сегодняшние скрипты временные** (`batch_loop.py`, `icp_dns_llm.py`,
  `mac_enrich_fast.py` в scratchpad сессии) — оформить как CLI-команду
  «пакет: названия → ICP → чтение» с ограничителем запросов и canary-проверкой
  блока.

## Вступления и лиды

Тест 2026-09-18: 10 первых из очереди, 2 аккаунта параллельно → 2 вступления
(agencybuilders, mvp — как выяснилось, под тестовым аккаунтом), 8 без
результата. Исправления — ветка `fix/auto-join-profile-step` (не влита).
Повторный тест на той же ветке: main вступил в agencybuilders (21 раздел) и
mvp (13), founders-run-club → `dead_host`, siliconslopes/engglobal исключены
(куки уже есть), bumbleb — вступление есть, но 0 разделов (платное).

- [ ] **Сменить пароль `CIRCLE_EMAIL`** — 2026-09-18 бот ввёл его на
  `login.ecommerce.pl` (сторонний сайт). Код исправлен (`fillCredential`),
  пароль — за пользователем. См. WORKLOG «Инцидент».
- [ ] **Тестовый аккаунт без профиля Circle** — его вступления (agencybuilders,
  mvp в профиле «Еркебулан») висят на «Create a profile»; у main профиль
  подтверждается сразу. Шаг профиля ботом проверить вживую на test.
- [ ] **Предпроверка мёртвых хостов не ловит редирект в браузере** —
  founders-run-club.circle.so отдаёт `requests` 401 без редиректа, мёртвый
  домен виден только браузеру (стоит одного визита). Приемлемо.
- [ ] **Платные с бесплатным входом без доступа** — bumbleb: участник, но 0
  разделов. Считать «вступили, читать нечего», не тратить на них визиты.

- [ ] **КРИТИЧНО: бот не заканчивает вступление** — после клика Circle ведёт
  на «Create a profile» (`/settings/profile?new_state=true`, кнопка
  Continue). Пока профиль не сохранён, любой запрос к API — `400 "Please
  confirm before proceeding"`, читать нельзя. agencybuilders и mvp — реальные
  вступления, застрявшие на этом шаге (проверено из браузера 2026-09-18).
  Бот должен дойти до Continue и считать `joined` только после ответа 200 от
  `/internal_api/spaces` изнутри страницы. `ego_join_driver.mjs`.
- [ ] **Скан по кукам молчит об ошибке** — на 400 `list_spaces` отдаёт пустой
  список, скан пишет `connected, 0 spaces`. Нужен отдельный статус
  («вступление не завершено»). `member_api_reader.py` `_get` (≥ 400).
- [ ] **Бот работает в личном профиле браузера** — оба спейса ego созданы в
  профиле `Default` («Еркебулан»). Спейсы делят куки своего профиля (док ego
  `references/clearing-state.md`), поэтому аккаунты разделяет только
  отдельный профиль на каждый: `taskSpace(name, {profileId})`. Профили через
  API не создаются — только в Ego Lite.

- [ ] **Сообщество не на английском — бот не видит кнопку** — ulule
  («Rejoindre», «Se connecter») → `unclear`. Добавить локализованные тексты
  кнопок (fr/de/es/pl…) в `JOIN_TEXTS` и поиск входа. `ego_join_driver.mjs`.
- [ ] **Скриншот/решение до загрузки страницы** — entrepreneur-bootcamp:
  на снимке только спиннер → `unclear`. Ждать, пока страница догрузится,
  перед классификацией состояния.
- [ ] **Ошибка открытия страницы валит попытку как «поломка браузера»** —
  `ERR_ABORTED` (bumbleb, ссылка `/join?invitation_token=…`),
  `ERR_NAME_NOT_RESOLVED` (founders-run-club — адреса нет). В обычном
  прогоне это останавливает всю пачку. Нужен отдельный исход (`dead_host` /
  `nav_failed`), запись в БД, пачка идёт дальше. `joiner.py` / `ego_bridge.py`.
- [ ] **Мёртвые хосты в очереди** — founders-run-club перенаправляет на
  `www.foundersrunclubs.com`, домен не резолвится (проверено пользователем).
  Проверять живость перед очередью.
- [ ] **Своя регистрация перед Circle** — ecommerce: `my.ecommerce.pl` →
  регистрация на `login.ecommerce.pl` → затем обычный Circle «Create a
  profile» (проверено пользователем). Бот такую форму не знает → `unclear`.
  Решить: регистрировать автоматически или отдавать человеку.
- [ ] **Очередь не знает о существующих подключениях** — siliconslopes уже
  `connected` (917 постов), engglobal уже есть в `circle_connections`, но оба
  в очереди на вступление как `not_attempted`. Исключать хосты с куками.
- [ ] **`needs_login` на своём домене** — icecampus: логин отправлен, сайт
  всё ещё просит войти (нет аккаунта на этом домене или 2FA).
- [ ] **Нет проверки, под каким аккаунтом вступили** — main и test в одном
  браузере ego; если хост уже залогинен другим аккаунтом, бот вступит под
  ним. Сверять email в профиле после вступления.
- [ ] **`member_label` не сохраняется** — `connect_host(..., member_label=)`
  после вступления, в `circle_connections` поле пустое.
- [ ] **Скрипт теста** передавал `--screenshot-dir` относительным путём —
  ego создаёт папку в своей рабочей директории и падает (`mkdir 'data'`).
  Делать путь абсолютным в `ego_bridge.py`.

- [ ] **Автоджойн выключен** — launchd не загружен, бот только на Mac.
- [ ] **Решить по платным** — 274 подходящих сообщества платные (270 из
  каталога без реального адреса).
- [ ] **Контакты авторов лидов** — без них лид не доходит до продаж.
- [ ] **Дашборд для заказчика** — показывать воронку группами по доступности,
  с датой обновления (промпт для отдельного чата подготовлен 2026-09-18).
